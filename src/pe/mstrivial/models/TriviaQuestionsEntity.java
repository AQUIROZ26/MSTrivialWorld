package pe.mstrivial.models;

import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.ArrayList;
import java.util.List;

public class TriviaQuestionsEntity extends BaseEntity{/*
    private static String DEFAULT_SQL = "SELECT * FROM trivialQuestions";

    private List<TriviaQuestion> findByCriteria(String sql, QuestionsEntity questionsEntity, PeopleEntity peopleEntity1, PeopleEntity peopleEntity2){
        List<TriviaQuestion> triviaQuestions = new ArrayList<>();
        if (getConnection() != null{
            try {
                ResultSet resultSet = getConnection().createStatement().executeQuery(sql);
                while (resultSet.next()){
                    TriviaQuestion triviaQuestion = new TriviaQuestion(
                            resultSet.getString("id"),

                    )
                }
            } catch (SQLException e) {
                e.printStackTrace();
            }
        }
    }*/
}
